import { CartContent } from "./cart-content";

export const metadata = {
  title: "Shopping Cart | LuxesJewel",
  description: "Review your selected items and proceed to checkout.",
};

export default function CartPage() {
  return <CartContent />;
}
