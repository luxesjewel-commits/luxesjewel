import { WishlistContent } from "./wishlist-content";

export const metadata = {
  title: "Wishlist | LuxesJewel",
  description: "Your saved items and favourites.",
};

export default function WishlistPage() {
  return <WishlistContent />;
}
